- Runs a custom review prompt with opencode (GH copilot anthropic haiku 4.5) on the diff of a branch vs target (default: current branch > staging).

## Usage

```bash
npm run scripts:prepare && ./scripts/public/review/main.sh
```
